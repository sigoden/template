{
  @client({
    name: "default",
    kind: "http",
    options: {
      baseURL: "http://localhost:3000",
      timeout: 1000,
    }
  })
  @mixin("mixin")
  @jslib("@rand")
  @module("auth")
  @module("post")
}